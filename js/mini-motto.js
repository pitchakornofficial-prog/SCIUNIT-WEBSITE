(function () {
  const el = document.getElementById("miniMotto");
  if (!el) return;

  // 🔁 ข้อความที่จะสุ่ม
  const mottos = [
    "วิทย์เพื่อทุกคน",
    "ฟังทุกเสียงของชาววิทย์",
    "ทีมที่พร้อมลงมือทำ",
    "รวมพลังนักศึกษาวิทยาศาสตร์",
    "เพื่ออนาคตที่ดีกว่าของคณะเรา",
  ];

  // 🎲 สุ่ม 1 ประโยค
  const randomIndex = Math.floor(Math.random() * mottos.length);
  el.textContent = mottos[randomIndex];
})();
